# ===============================
# Application
# ===============================
spring.application.name=ClinicExperts

# ===============================
# Server (Render compatible)
# ===============================
server.port=${PORT}

# ===============================
# PostgreSQL Database (Render / Live)
# ===============================
spring.datasource.url=jdbc:postgresql://dpg-d60suo9r0fns73cdnh00-a:5432/clinic_db_n6y0
spring.datasource.username=clinic_db_n6y0_user
spring.datasource.password=HJDqMrqPCV8q8e1CpVyjj9DfvY5oFMuP
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

#  Production safe (NO auto schema changes)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# ===============================
# Logging (Production Safe)
# ===============================
logging.level.root=INFO
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=OFF

logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} %-5level %logger{36} - %msg%n

# ===============================
# Actuator (Minimal Exposure)
# ===============================
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never

# ===============================
# Swagger (DISABLED in production)
# ===============================
springdoc.swagger-ui.enabled=false
springdoc.api-docs.enabled=false

# ===============================
# Email (Gmail SMTP via Env Vars)
# ===============================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# ===============================
# Application URLs
# ===============================
app.base-url=${APP_BASE_URL}
app.frontend-url=${APP_FRONTEND_URL}

# ===============================
# AOP
# ===============================
spring.aop.auto=true
spring.aop.proxy-target-class=true
logging.level.com.clinic.Aspect=INFO
